import matplotlib.pyplot as plt
from skimage import data
import numpy as np
import math

# lenna = data.astronaut()
# row= lenna.shape[0]
#
# col = lenna.shape[1]
# lenna_rotation = np.zeros_like(lenna)
# x=math.pi
# A = np.mat([[math.cos(x), -math.sin(x)], [math.sin(x), math.cos(x)]])
#
# for r in range(row):
#     for l in range(col):
#         v = np.dot(A.I, np.array([r, l]).T)
#
#         lenna_rotation[r, l] = lenna[int(v[0, 0]), int(v[0, 1])]
#
# plt.imshow(lenna_rotation)
# plt.show()

# src=np.array([[1, 1, 1],[1, -7, 1],[1, 1, 1]])
# src_h, src_w = src.shape[:2]
#
# print(src_h)
class Calcu():
    def jishuan(a,b):
        return a+b
    if __name__ == '__main__':jishuan



    def sub(a,b):
        return a-b
    if __name__ == '__main__':sub

    def div(a,b):
        return a/b

    if __name__ == '__main__': div